version: '3'

services:
  serve:
    build: .
    image: serve-molecula-documentation-jekyll
    container_name: docs-site-container
    command: jekyll serve --watch --force_polling --verbose --livereload
    volumes:
      - .:/srv/jekyll:consistent
    ports:
      - '4000:4000'
    expose:
      - '4000'
